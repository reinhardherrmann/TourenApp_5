package de.orome.tourenapp5.application

import android.app.Application

class TourenApplication: Application() {

    //private val dataBase by lazy { TourenDatabase.getDatabase((this@TourenApplication)) }

    //val loginRepository by lazy { LoginRepository(dataBase.userDao, dataBase.lagerDao) }
}